let yashu=[
    '1',
    '2',
    '3',
    '4',
    '5'
]

for( let i=0;i<yashu.length;i++)
{
    if(yashu[i]%2==0)
    {
    console.log(`Even no is ${yashu[i]}`)
    }
    else
    {
        console.log(`odd no is ${yashu[i]}`)
    }
}
